---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`prose prose-invert max-w-none prose-a:text-brand-cyan prose-a:underline-offset-4 prose-strong:text-white prose-headings:text-white ${className}`}>
  <slot />
</div>
