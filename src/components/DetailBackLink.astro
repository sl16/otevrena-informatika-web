---
interface Props {
  hrefBack: string;
  labelBack: string;
}

const { hrefBack, labelBack } = Astro.props;
---

<a
  href={hrefBack}
  class="inline-flex items-center gap-2 text-xs font-mono uppercase tracking-[0.24em] text-slate-500 hover:text-white transition-colors"
>
  ⬅️ {labelBack}
</a>